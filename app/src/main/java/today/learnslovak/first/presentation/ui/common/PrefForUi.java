package today.learnslovak.first.presentation.ui.common;

import today.learnslovak.first.domain.model.Preferences;

public enum PrefForUi implements Preferences {
  KEEP_SCREEN_ON, FONT_SIZE, FAB_SIZE, WORDS_PAUSE_DURATION, WORDS_MODE
}
